{
  "name": "proyecto-graphics",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview --host --port $PORT"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
